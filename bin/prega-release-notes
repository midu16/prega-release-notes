#!/usr/bin/env bash
# CLI entrypoint for the prega-release-notes repository (subset catalog release notes).
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$ROOT/scripts/release_notes_subset.py" "$@"
